global !p
def complete(t, options):
    if t:
        options = [ m[len(t):] for m in options if m.startswith(t) ]
    if len(options) == 1:
        return options[0]
    return "(" + '|'.join(options) + ")"
endglobal

snippet fu "CP Team PHP function style [override]" !b
function ${1:name}(${2:$param}) {
    ${VISUAL}${3:return null;}
}
$0
endsnippet
